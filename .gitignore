# Build output is committed on purpose: the gh-pages branch is cut from dist/ with
# `git subtree push --prefix dist origin gh-pages`, which needs dist/ tracked in history.
node_modules/
turbo-circuit.zip
npm-debug.log*
*.log
Thumbs.db
.DS_Store
# verification artifact — regenerated by every `npm run deploy` at the live URL
shots/dist-live-deploy.png
